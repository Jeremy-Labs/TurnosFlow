"use client";
import { useState } from "react";
export function AppointmentStatus({ id, status }: { id: string; status: string }) { const [value, setValue] = useState(status); const update = async (next: string) => { const response = await fetch(`/api/appointments/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status: next }) }); if (response.ok) setValue(next); }; return <select aria-label="Estado del turno" className="rounded-full border px-3 py-1 text-sm" value={value} onChange={(e) => update(e.target.value)}><option value="PENDING">PENDING</option><option value="CONFIRMED">CONFIRMED</option><option value="COMPLETED">COMPLETED</option><option value="CANCELLED">CANCELLED</option><option value="NO_SHOW">NO_SHOW</option></select>; }
